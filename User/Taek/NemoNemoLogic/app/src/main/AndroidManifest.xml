<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nemonemologic" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NemoNemoLogic" >

        <activity
            android:name=".Title"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--팝업창 테마 설정-->
        <activity android:name=".Title_exit_popup" android:theme="@android:style/Theme.Dialog"/>

        <activity android:name=".Level.Level_select"/>
        <activity android:name=".Level.Level_1"/>
        <activity android:name=".Level.Level_2"/>
        <activity android:name=".Level.Level_3"/>

    </application>

</manifest>